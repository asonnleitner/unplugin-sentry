{
  "private": true,
  "scripts": {
    "dev": "nodemon --watch '../../src/**/*.ts' --ext .ts --exec 'vite'",
    "dev:build": "nodemon --watch '../../src/**/*.ts' --ext .ts --exec 'vite build'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.5",
    "vite-plugin-inspect": "^0.5.0"
  }
}
